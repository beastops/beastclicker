<Application x:Class="BeastClicker.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             StartupUri="MainWindow.xaml">
  <Application.Resources>

    <!--
      Soft light theme. The previous classic-Win32 chrome (etched frames, #707070
      hard edges, mid-grey button faces) read as dated, so borders are lightened,
      corners take a small radius, and contrast is carried by type rather than by
      heavy outlines. Restrained on purpose: one accent, no gradients, no
      decoration that isn't doing work.
    -->

    <!-- surfaces -->
    <SolidColorBrush x:Key="Canvas"    Color="#F6F7F9"/>
    <SolidColorBrush x:Key="Surface"   Color="#FFFFFF"/>
    <SolidColorBrush x:Key="Hairline"  Color="#E6E9EF"/>
    <SolidColorBrush x:Key="Bar"       Color="#FBFCFD"/>

    <!-- type -->
    <SolidColorBrush x:Key="TextPri"   Color="#1F2430"/>
    <SolidColorBrush x:Key="TextSec"   Color="#6B7280"/>
    <SolidColorBrush x:Key="TextHint"  Color="#98A0AC"/>

    <!-- interactive -->
    <SolidColorBrush x:Key="CtrlFace"  Color="#F4F6F9"/>
    <SolidColorBrush x:Key="CtrlEdge"  Color="#D6DBE3"/>
    <SolidColorBrush x:Key="CtrlHover" Color="#EDF2FA"/>
    <SolidColorBrush x:Key="CtrlPress" Color="#E2EAF6"/>
    <SolidColorBrush x:Key="Accent"    Color="#3B7DD8"/>
    <SolidColorBrush x:Key="AccentDim" Color="#9CBCE8"/>
    <SolidColorBrush x:Key="OffFace"   Color="#F2F3F5"/>
    <SolidColorBrush x:Key="OffEdge"   Color="#E3E6EB"/>

    <!-- primary actions: muted, not candy -->
    <SolidColorBrush x:Key="GoFace"    Color="#E7F4EC"/>
    <SolidColorBrush x:Key="GoEdge"    Color="#8CBFA1"/>
    <SolidColorBrush x:Key="GoHover"   Color="#DCEFE4"/>
    <SolidColorBrush x:Key="GoPress"   Color="#CDE7D8"/>
    <SolidColorBrush x:Key="GoText"    Color="#1E5537"/>
    <SolidColorBrush x:Key="StopFace"  Color="#FBECEC"/>
    <SolidColorBrush x:Key="StopEdge"  Color="#D8A3A6"/>
    <SolidColorBrush x:Key="StopHover" Color="#F7E2E2"/>
    <SolidColorBrush x:Key="StopPress" Color="#F0D2D3"/>
    <SolidColorBrush x:Key="StopText"  Color="#6E2327"/>

    <!-- ===================== panel (templated GroupBox) ================== -->
    <!-- Same GroupBox element the layout already uses, redrawn as a soft panel
         with a plain header instead of an etched frame with a notch. -->
    <Style TargetType="GroupBox">
      <Setter Property="Margin" Value="0,0,0,10"/>
      <Setter Property="Padding" Value="14,10,14,13"/>
      <Setter Property="Foreground" Value="{StaticResource TextPri}"/>
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="GroupBox">
            <Border Background="{StaticResource Surface}"
                    BorderBrush="{StaticResource Hairline}" BorderThickness="1"
                    CornerRadius="7" SnapsToDevicePixels="True">
              <StackPanel>
                <TextBlock Text="{TemplateBinding Header}" FontSize="12"
                           FontWeight="SemiBold" Foreground="{StaticResource TextPri}"
                           Margin="14,11,14,0"/>
                <ContentPresenter Margin="{TemplateBinding Padding}"/>
              </StackPanel>
            </Border>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>

    <!-- ============================= Button ============================= -->
    <Style TargetType="Button">
      <Setter Property="FontSize" Value="12"/>
      <Setter Property="Foreground" Value="{StaticResource TextPri}"/>
      <Setter Property="Height" Value="30"/>
      <Setter Property="Padding" Value="12,0"/>
      <Setter Property="SnapsToDevicePixels" Value="True"/>
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="Button">
            <Border x:Name="b" Background="{StaticResource CtrlFace}"
                    BorderBrush="{StaticResource CtrlEdge}" BorderThickness="1"
                    CornerRadius="5" SnapsToDevicePixels="True">
              <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"
                                Margin="{TemplateBinding Padding}"/>
            </Border>
            <ControlTemplate.Triggers>
              <Trigger Property="IsMouseOver" Value="True">
                <Setter TargetName="b" Property="Background" Value="{StaticResource CtrlHover}"/>
                <Setter TargetName="b" Property="BorderBrush" Value="{StaticResource AccentDim}"/>
              </Trigger>
              <Trigger Property="IsPressed" Value="True">
                <Setter TargetName="b" Property="Background" Value="{StaticResource CtrlPress}"/>
                <Setter TargetName="b" Property="BorderBrush" Value="{StaticResource Accent}"/>
              </Trigger>
              <Trigger Property="IsKeyboardFocused" Value="True">
                <Setter TargetName="b" Property="BorderBrush" Value="{StaticResource Accent}"/>
              </Trigger>
              <Trigger Property="IsEnabled" Value="False">
                <Setter TargetName="b" Property="Background" Value="{StaticResource OffFace}"/>
                <Setter TargetName="b" Property="BorderBrush" Value="{StaticResource OffEdge}"/>
                <Setter Property="Foreground" Value="{StaticResource TextHint}"/>
              </Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>

    <Style x:Key="GoButton" TargetType="Button">
      <Setter Property="FontSize" Value="12.5"/>
      <Setter Property="FontWeight" Value="SemiBold"/>
      <Setter Property="Foreground" Value="{StaticResource GoText}"/>
      <Setter Property="Height" Value="36"/>
      <Setter Property="SnapsToDevicePixels" Value="True"/>
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="Button">
            <Border x:Name="b" Background="{StaticResource GoFace}"
                    BorderBrush="{StaticResource GoEdge}" BorderThickness="1"
                    CornerRadius="5" SnapsToDevicePixels="True">
              <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Border>
            <ControlTemplate.Triggers>
              <Trigger Property="IsMouseOver" Value="True">
                <Setter TargetName="b" Property="Background" Value="{StaticResource GoHover}"/>
              </Trigger>
              <Trigger Property="IsPressed" Value="True">
                <Setter TargetName="b" Property="Background" Value="{StaticResource GoPress}"/>
              </Trigger>
              <Trigger Property="IsEnabled" Value="False">
                <Setter TargetName="b" Property="Background" Value="{StaticResource OffFace}"/>
                <Setter TargetName="b" Property="BorderBrush" Value="{StaticResource OffEdge}"/>
                <Setter Property="Foreground" Value="{StaticResource TextHint}"/>
              </Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>

    <Style x:Key="StopButton" TargetType="Button" BasedOn="{StaticResource GoButton}">
      <Setter Property="Foreground" Value="{StaticResource StopText}"/>
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="Button">
            <Border x:Name="b" Background="{StaticResource StopFace}"
                    BorderBrush="{StaticResource StopEdge}" BorderThickness="1"
                    CornerRadius="5" SnapsToDevicePixels="True">
              <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Border>
            <ControlTemplate.Triggers>
              <Trigger Property="IsMouseOver" Value="True">
                <Setter TargetName="b" Property="Background" Value="{StaticResource StopHover}"/>
              </Trigger>
              <Trigger Property="IsPressed" Value="True">
                <Setter TargetName="b" Property="Background" Value="{StaticResource StopPress}"/>
              </Trigger>
              <Trigger Property="IsEnabled" Value="False">
                <Setter TargetName="b" Property="Background" Value="{StaticResource OffFace}"/>
                <Setter TargetName="b" Property="BorderBrush" Value="{StaticResource OffEdge}"/>
                <Setter Property="Foreground" Value="{StaticResource TextHint}"/>
              </Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>

    <!-- ============================ TextBox ============================= -->
    <Style TargetType="TextBox">
      <Setter Property="FontSize" Value="12"/>
      <Setter Property="Height" Value="27"/>
      <Setter Property="Padding" Value="6,0"/>
      <Setter Property="Foreground" Value="{StaticResource TextPri}"/>
      <Setter Property="CaretBrush" Value="{StaticResource TextPri}"/>
      <Setter Property="SelectionBrush" Value="{StaticResource AccentDim}"/>
      <Setter Property="VerticalContentAlignment" Value="Center"/>
      <Setter Property="HorizontalContentAlignment" Value="Center"/>
      <Setter Property="SnapsToDevicePixels" Value="True"/>
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="TextBox">
            <Border x:Name="b" Background="{StaticResource Surface}"
                    BorderBrush="{StaticResource CtrlEdge}" BorderThickness="1"
                    CornerRadius="5" SnapsToDevicePixels="True">
              <!-- Alignment comes from VerticalContentAlignment; forcing
                   VerticalAlignment here clips digits at this height. -->
              <ScrollViewer x:Name="PART_ContentHost" Margin="{TemplateBinding Padding}"/>
            </Border>
            <ControlTemplate.Triggers>
              <Trigger Property="IsMouseOver" Value="True">
                <Setter TargetName="b" Property="BorderBrush" Value="{StaticResource AccentDim}"/>
              </Trigger>
              <Trigger Property="IsKeyboardFocused" Value="True">
                <Setter TargetName="b" Property="BorderBrush" Value="{StaticResource Accent}"/>
              </Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>

    <!-- =========================== ComboBox ============================= -->
    <Style x:Key="ComboToggle" TargetType="ToggleButton">
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="ToggleButton">
            <Grid SnapsToDevicePixels="True">
              <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/><ColumnDefinition Width="22"/>
              </Grid.ColumnDefinitions>
              <Border x:Name="b" Grid.ColumnSpan="2" Background="{StaticResource Surface}"
                      BorderBrush="{StaticResource CtrlEdge}" BorderThickness="1"
                      CornerRadius="5"/>
              <Path x:Name="arrow" Grid.Column="1" HorizontalAlignment="Center"
                    VerticalAlignment="Center" Fill="{StaticResource TextSec}"
                    Data="M 0 0 L 7 0 L 3.5 4 Z"/>
            </Grid>
            <ControlTemplate.Triggers>
              <Trigger Property="IsMouseOver" Value="True">
                <Setter TargetName="b" Property="BorderBrush" Value="{StaticResource AccentDim}"/>
              </Trigger>
              <Trigger Property="IsChecked" Value="True">
                <Setter TargetName="b" Property="BorderBrush" Value="{StaticResource Accent}"/>
              </Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>

    <Style TargetType="ComboBox">
      <Setter Property="FontSize" Value="12"/>
      <Setter Property="Height" Value="27"/>
      <Setter Property="Foreground" Value="{StaticResource TextPri}"/>
      <Setter Property="SnapsToDevicePixels" Value="True"/>
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="ComboBox">
            <Grid>
              <ToggleButton Style="{StaticResource ComboToggle}" Focusable="False" ClickMode="Press"
                            IsChecked="{Binding IsDropDownOpen, Mode=TwoWay,
                                        RelativeSource={RelativeSource TemplatedParent}}"/>
              <ContentPresenter Margin="9,0,26,0" VerticalAlignment="Center"
                                HorizontalAlignment="Left" IsHitTestVisible="False"
                                Content="{TemplateBinding SelectionBoxItem}"
                                ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"/>
              <Popup x:Name="PART_Popup" AllowsTransparency="True" Placement="Bottom"
                     IsOpen="{TemplateBinding IsDropDownOpen}" Focusable="False" PopupAnimation="Fade">
                <Border Background="{StaticResource Surface}" BorderBrush="{StaticResource CtrlEdge}"
                        BorderThickness="1" CornerRadius="5" Padding="2" Margin="0,3,0,0"
                        SnapsToDevicePixels="True"
                        MinWidth="{TemplateBinding ActualWidth}"
                        MaxHeight="{TemplateBinding MaxDropDownHeight}">
                  <ScrollViewer>
                    <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Contained"/>
                  </ScrollViewer>
                </Border>
              </Popup>
            </Grid>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>

    <Style TargetType="ComboBoxItem">
      <Setter Property="FontSize" Value="12"/>
      <Setter Property="Padding" Value="8,6"/>
      <Setter Property="Foreground" Value="{StaticResource TextPri}"/>
      <Setter Property="SnapsToDevicePixels" Value="True"/>
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="ComboBoxItem">
            <Border x:Name="b" Background="Transparent" CornerRadius="4"
                    Padding="{TemplateBinding Padding}" SnapsToDevicePixels="True">
              <ContentPresenter/>
            </Border>
            <ControlTemplate.Triggers>
              <Trigger Property="IsMouseOver" Value="True">
                <Setter TargetName="b" Property="Background" Value="{StaticResource CtrlHover}"/>
              </Trigger>
              <Trigger Property="IsSelected" Value="True">
                <Setter TargetName="b" Property="Background" Value="{StaticResource CtrlPress}"/>
              </Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>

    <!-- ===================== radio / check (softened) ==================== -->
    <Style TargetType="RadioButton">
      <Setter Property="FontSize" Value="12"/>
      <Setter Property="Foreground" Value="{StaticResource TextPri}"/>
      <Setter Property="VerticalContentAlignment" Value="Center"/>
      <Setter Property="Cursor" Value="Hand"/>
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="RadioButton">
            <StackPanel Orientation="Horizontal" Background="Transparent">
              <Grid Width="15" Height="15" VerticalAlignment="Center">
                <Ellipse x:Name="ring" Stroke="{StaticResource CtrlEdge}" StrokeThickness="1.3"
                         Fill="{StaticResource Surface}"/>
                <Ellipse x:Name="dot" Width="7" Height="7" Fill="{StaticResource Accent}"
                         Visibility="Collapsed"/>
              </Grid>
              <ContentPresenter Margin="8,0,0,0" VerticalAlignment="Center"/>
            </StackPanel>
            <ControlTemplate.Triggers>
              <Trigger Property="IsChecked" Value="True">
                <Setter TargetName="ring" Property="Stroke" Value="{StaticResource Accent}"/>
                <Setter TargetName="dot" Property="Visibility" Value="Visible"/>
              </Trigger>
              <Trigger Property="IsMouseOver" Value="True">
                <Setter TargetName="ring" Property="Stroke" Value="{StaticResource Accent}"/>
              </Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>

    <Style TargetType="CheckBox">
      <Setter Property="FontSize" Value="12"/>
      <Setter Property="Foreground" Value="{StaticResource TextPri}"/>
      <Setter Property="VerticalContentAlignment" Value="Center"/>
      <Setter Property="Cursor" Value="Hand"/>
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="CheckBox">
            <StackPanel Orientation="Horizontal" Background="Transparent">
              <Border x:Name="box" Width="15" Height="15" CornerRadius="4"
                      BorderBrush="{StaticResource CtrlEdge}" BorderThickness="1.3"
                      Background="{StaticResource Surface}" VerticalAlignment="Center">
                <Path x:Name="tick" Stroke="White" StrokeThickness="1.8" Visibility="Collapsed"
                      StrokeStartLineCap="Round" StrokeEndLineCap="Round"
                      Data="M 3 7 L 6 10 L 11 4" SnapsToDevicePixels="True"/>
              </Border>
              <ContentPresenter Margin="8,0,0,0" VerticalAlignment="Center"/>
            </StackPanel>
            <ControlTemplate.Triggers>
              <Trigger Property="IsChecked" Value="True">
                <Setter TargetName="box" Property="Background" Value="{StaticResource Accent}"/>
                <Setter TargetName="box" Property="BorderBrush" Value="{StaticResource Accent}"/>
                <Setter TargetName="tick" Property="Visibility" Value="Visible"/>
              </Trigger>
              <Trigger Property="IsMouseOver" Value="True">
                <Setter TargetName="box" Property="BorderBrush" Value="{StaticResource Accent}"/>
              </Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>

    <Style TargetType="TextBlock">
      <Setter Property="FontSize" Value="12"/>
      <Setter Property="VerticalAlignment" Value="Center"/>
      <Setter Property="Foreground" Value="{StaticResource TextSec}"/>
    </Style>

  </Application.Resources>
</Application>
